let numberOfBooks=parseInt(readLine());
let books=[];
let querry=[];
let count=0
for(let i=0;i<numberOfBooks;i++){
    let inward=ReadLine();
    books[i]=inward;
}
let req=parseInt(readLine())
for(let j=0;j<req;j++){
    let givenList=readLine()
    querry[i]=givenList;
}
for(let i=0;i<req;i++){
    for(let j=0;j<mberOfBooks;j++){
        if ((querry[i])==books[j]){
            count+=1
        }
        else if()
    }
}